{
  "name": "my_random_package",
  "version": "1.0.0",
  "description": "This Package provides the following: generation of a random integer, generate a random floating point number, generation of a random string and random key generation of specified length.",
  "repository": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ivan Kalugin",
  "license": "ISC"
}
